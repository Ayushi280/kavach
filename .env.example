# Copy this file to .env and fill in real values to switch off mock data.
# Both are optional — with no .env the app runs entirely on mock data.

# Set to "false" to hit the real backend
VITE_USE_MOCK=true

# FastAPI backend base URL
VITE_API_BASE=http://localhost:8000
